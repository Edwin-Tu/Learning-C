# 📘 C 課程練習檔案說明

## assignment15.c
**主題：** 指標與記憶體位址的操作  
**內容：**  
- 宣告整數變數 `int variable = 15;`  
- 宣告指標 `int *ptr = &variable;` 並指向 `variable` 的記憶體位址  
- 使用 `printf` 輸出：
  1. 變數的值 (`variable`)  
  2. 指標的值 (即 `variable` 的記憶體位址)  
  3. 直接印出變數的記憶體位址 (`&variable`)  
  4. 解參考指標 (`*ptr`)，輸出記憶體位址中存的值